
/**
 * 元素配置映射
 * e.g:
 *   ${ELEMENT}: ${ID}
 *   ELEMENT 
 *    - format: ${SYSTEM}：${MODULE}：${ACTION}
 */

module.exports = {
  // 商家模块
  // 删除设备
  'BUSINESS:DEVICE:CANCEL': 1,
  // 新增设备  
  'BUSINESS:DEVICE:ADD': 4,
  // 展示设备返厂信息  
  'BUSINESS:DEVICE:SHOW_RESTROFITY': 3,
};
